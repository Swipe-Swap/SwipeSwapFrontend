import 'package:flutter/material.dart';

class DiningCourtWidget extends StatelessWidget {
  const DiningCourtWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Stack(
      children: [],
    );
  }
}
